import { useTheme } from '@/contexts/ThemeContext';
import { Sun, Moon } from 'lucide-react';

export default function ThemeToggle() {
  // Tema değiştirme geçici olarak devre dışı bırakıldı (varsayılan: dark)
  return null;
}
